import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';  // Opcional si tienes estilos CSS
import App from './App'; // Asegúrate de tener este archivo también


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
